%h2 
  = "Welcome to Handicappr, " + @user.name
%p Start by entering the slope, rating and score from your recent rounds of golf

- semantic_form_for @round do |form|
  = form.error_messages
  = render :partial => "/rounds/simple_form", :locals => { :form => form }
  = form.buttons
    
%h3 Past rounds:
%table
  %thead
    %tr
      %th Slope
      %th Rating
      %th Score
      %th Differential
  %tbody
    - @user.recent_rounds.each_with_index do |r, i|
      %tr{:class => (i < @user.num_rounds_to_average) ? "used" : ""}
        %td= r.slope
        %td= r.rating
        %td= r.score
        %td= r.differential
  
- content_for :sidebar do
  %p Your Handicap is:
  = render :partial => "users/handicap"